using System;

namespace ServiceAuto
{
    // Scrieți o aplicație de consolă pentru gestiunea unui service auto.Veți avea următoarele funcționalități:

    // 1.Intrare în service: id intrare, număr stand, număr mașină, număr zile. 
    // Numărul standului trebuie să fie al unui stand neocupat de altă mașină. 
    // Numărul de zile trebuie să fie strict pozitiv. Numărul mașinii trebuie să fie valid. ID-ul trebuie să fie unic.

    // 2.Ieșire din service: se dă un număr de stand, un raport și un preț facturat. 
    // Numărul de stand trebuie să existe, iar prețul facturat este strict pozitiv.

    // 3.Afișare standuri: afișați standurile ordonate descrescător după prețul facturat (ca medie). Se va afișa numărul standului și media de preț.

    class Startup
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Hello World!");
        }
    }
}
